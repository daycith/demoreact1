import React from "react";

import {Button as BButton} from "react-bootstrap";

const Button4= ({text}) => {
    
    return (
    <BButton >
        {text}    
    </BButton>
    )
}

export default Button4;
